# these are our own config values defined by the app
dbconnection {
    driver="com.mysql.cj.jdbc.Driver"
    url="jdbc:mysql://myhostname/mydbname"
    username="xxxxxxxxxxxxx"
    password="xxxxxxxxxxxx"
}

general {
    ## This can be the name of a file in the resources directory OR
    ## the full path to another file on the filesystem. The code checks
    ## the resource directory first
    wetpathfile="wet.paths.201612"
}

